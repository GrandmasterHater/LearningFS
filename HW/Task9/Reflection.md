Решения полностью совпадают с эталоном. Функции зеркальны: curry предоставляет каррированную функцию, uncurry - наоборот позволяет получить 
функцию, которая принимает пару аргументов и передаёт в карриованную функцию.
